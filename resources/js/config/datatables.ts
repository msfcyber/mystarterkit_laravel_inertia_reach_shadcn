/** Default `useDataTable` bila `searchDebounceMs` tidak di-set di `DataTable`. */
export const DATA_TABLE_DEFAULT_SEARCH_DEBOUNCE_MS = 400;

/** Debounce pencarian server-side halaman management (users, roles, permissions). */
export const MANAGEMENT_DATA_TABLE_SEARCH_DEBOUNCE_MS = 500;

/** Activity log: query lebih berat — debounce sedikit lebih lama. */
export const ACTIVITY_LOG_DATA_TABLE_SEARCH_DEBOUNCE_MS = 650;
